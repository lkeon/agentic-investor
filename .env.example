# Required by the visualisation and pipeline. Use Neon's pooled URL for the app.
DATABASE_URL=postgresql+psycopg://user:password@endpoint-pooler.region.aws.neon.tech/database_name?sslmode=require

# Required only for extraction and embedding commands.
OPENAI_API_KEY=your-api-key
